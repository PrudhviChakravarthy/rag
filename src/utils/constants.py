import os
from pathlib import Path

TEMP_DIR = Path("temp_uploads")
PICKLE_FOLDER = os.getcwd()
FILES_JSON_PATH = Path("files.json")
HISTORY_JSON_DIR = Path("history")
HISTORY_JSON_PATH = Path("history.json")